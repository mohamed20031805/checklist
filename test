# APRÈS — nom = label tâche (tronqué à 28 car Excel limite à 31 chars)
raw_name = task_name[:28].strip().replace("/", "-").replace("\\", "-").replace("*", "").replace("?", "").replace("[", "").replace("]", "").replace(":", "")
sname    = raw_name if raw_name not in [s.title for s in wb.worksheets] else f"{raw_name[:24]}_{doc_idx}"
doc_ref  = f"→ Feuille '{sname}'"
self._add_doc_sheet(wb, sname, imp_full, sec_name, task_name, imp_name)
doc_idx += 1
